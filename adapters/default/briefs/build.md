---
id: build
description: Apply the standard build flow without capability-specific generators
needs: [define]
---

The default capability does not bundle generators, reviewers, or adoption tools.

If implementation work is attached to this slice, follow the repository's ordinary development and verification instructions. Otherwise, treat this phase as a no-op and record that there is no capability-specific build action to perform.
