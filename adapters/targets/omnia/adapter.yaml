# yaml-language-server: $schema=https://raw.githubusercontent.com/augentic/specify-cli/main/schemas/target.schema.json
name: omnia
version: 1
axis: target
description: Omnia Rust WASM target adapter. Supplies idiom guidance for core synthesis (shape), drives crate / test / guest generation and code review (build), and gates the slice landing with cargo + wasm32-wasip2 verification (merge).
briefs:
  shape: briefs/shape.md
  build: briefs/build.md
  merge: briefs/merge.md
