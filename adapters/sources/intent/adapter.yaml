# yaml-language-server: $schema=https://github.com/augentic/specify-cli/raw/main/schemas/source.schema.json
name: intent
version: 1
axis: source
operations: [enumerate, extract]
description: >-
  Operator-supplied free-form intent. Degenerate N=1 source — one candidate per
  slice, one `kind: intent` claim per Evidence — that backs every plan including
  pure-greenfield work.
briefs:
  enumerate: briefs/enumerate.md
  extract: briefs/extract.md
