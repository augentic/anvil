{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/augentic/specify/schemas/adapter.schema.json",
  "title": "Specify adapter manifest",
  "description": "Shared shape for source and target adapter manifests (`adapter.yaml`) per workflow §Adapter implementation shape. Carries the kebab-case `name`, semver `version`, the `axis` discriminator (`source` or `target`), and the required `description`. Operation sets are not declared on the wire — they derive from the closed WIT contract (`wit/specify.wit`) per axis.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "version", "axis", "description"],
  "properties": {
    "name": {
      "$ref": "#/$defs/kebabName",
      "description": "Adapter identifier in kebab-case. Must match the directory name under sources/ or targets/."
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+(-[0-9A-Za-z.-]+)?(\\+[0-9A-Za-z.-]+)?$",
      "description": "Semver adapter version (`x.y.z` with optional `-prerelease` / `+build`). Resolution keys on this string as the adapter's identity."
    },
    "specify": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+(-[0-9A-Za-z.-]+)?(\\+[0-9A-Za-z.-]+)?$",
      "description": "Optional host-CLI compatibility floor (RFC-47 D3): the exact minimum `specify` platform version this adapter needs."
    },
    "axis": {
      "type": "string",
      "enum": ["source", "target"],
      "description": "Adapter role. `source` adapters survey leads and extract evidence; `target` adapters serve `shape`, `build`, and `merge` operations."
    },
    "inputs": {
      "type": "array",
      "items": { "$ref": "#/$defs/buildInputDeclaration" },
      "description": "Optional adapter-declared build inputs. Target-only: the per-axis `target.schema.json` admits the key while `source.schema.json` rejects it; the shared shape carries the property so the closed key set admits it. Each entry is a path relative to the build request's `inputs.root`, flagged `required`."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable, single-sentence summary of the adapter's role. Required."
    },
    "platforms": {
      "type": "object",
      "description": "Optional declarative platforms capability. Target-only: the per-axis `target.schema.json` refines the shape with `$defs/platformsCapability`; `source.schema.json` rejects the key. The shared shape carries the property so the closed key set (from `additionalProperties: false`) admits it."
    }
  },
  "$defs": {
    "kebabName": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Kebab-case identifier: lowercase letters and digits grouped by single internal hyphens; no leading, trailing, or consecutive hyphens."
    },
    "buildInputDeclaration": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "required"],
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1,
          "pattern": "^(?!/)(?![A-Za-z][A-Za-z0-9+.-]*://).+$",
          "description": "Build input path relative to the build request's `inputs.root` (the slice tree). Absolute paths and URIs are rejected."
        },
        "required": {
          "type": "boolean",
          "description": "Whether `build` requires this input; a missing `required` path is a build-time abort."
        }
      }
    }
  }
}
