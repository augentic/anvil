# Acceptance fixture for scenario 'contracts-describe' (C05 smoke).
# Placeholder OpenAPI document. The fixture backend materialises this
# file into the runner workspace so the 'files-exist' assertion has
# something to observe. Real OpenAPI authoring is exercised by the live
# agent backend (later changes in the implementation plan).
openapi: 3.1.0
info:
  title: Profile API (placeholder)
  version: 0.0.0
paths: {}
