# Install: `cargo install cargo-make`
# Help: https://sagiegurari.github.io/cargo-make/

# extend = [{ path = "examples/Makefile.toml" }]

[env]
CARGO_MAKE_EXTEND_WORKSPACE_MAKEFILE = true

[config]
default_to_workspace = true
skip_core_tasks = true
skip_crate_env_info = true
skip_git_env_info = true
skip_rust_env_info = true

# -------------------------------------
# CI Checks
# -------------------------------------
[tasks.check]
dependencies = ["fmt", "lint", "wasm", "test", "test-docs", "doc"]

[tasks.ci]
dependencies = ["check", "vet", "deny"]

# -------------------------------------
# Individual Actions
# -------------------------------------

# Audit
[tasks.audit]
command = "cargo"
args = ["audit"]

# Deny
[tasks.deny]
command = "cargo"
args = ["deny", "check"]
install_crate = "cargo-deny"

# Deps
[tasks.deps]
command = "rustup"
args = ["run", "nightly", "cargo", "udeps", "--all-targets"]
install_crate = "cargo-udeps"

# Fmt — mutates; the gates use fmt
[tasks.fmt]
command = "rustup"
args = ["run", "nightly", "cargo", "fmt", "--all"]

# Lint — --workspace so the examples package stays linted, matching CI
[tasks.lint]
command = "cargo"
args = [
    "clippy",
    "--locked",
    "--workspace",
    "--all-targets",
    "--all-features",
    "--",
    "-D",
    "warnings",
]
install_crate = { rustup_component_name = "clippy" }

# Outdated
[tasks.outdated]
command = "cargo"
args = ["outdated", "--depth", "1"]
install_crate = "cargo-outdated"

# Check wasm core — the per-push WASM boundary gate: the shipped guest
# crates plus the examples adapter shim. The adapter check lives in
# examples/Makefile.toml; this task keeps `cargo make ci` one-shot.
[tasks.wasm]
script_runner = "@shell"
script = '''
cargo check --locked --lib -p specify -p transport --target wasm32-wasip2
(cd examples && cargo make wasm)
'''
env = { RUSTFLAGS = "-Dwarnings" }

# Test — the ordinary suite over the default workspace members
# (crates/*, examples, tests); the WASM boundary
# smoke is opt-in via `cd examples && cargo make test-wasm`.
[tasks.test]
command = "cargo"
args = ["nextest", "run", "--locked", "--all-features", "--no-tests=pass"]
env = { RUSTFLAGS = "-Dwarnings" }

# Test Doc
[tasks.test-docs]
command = "cargo"
args = ["test", "--doc", "--workspace", "--all-features"]

# Docs
[tasks.doc]
command = "cargo"
args = ["doc", "--no-deps", "--workspace", "--all-features", "--locked"]
env = { RUSTDOCFLAGS = "-Dwarnings" }

# Miri
[tasks.miri]
script = '''
cargo +nightly miri setup
cargo +nightly miri nextest run --no-tests=pass
'''

# Vet
[tasks.vet]
script = '''
cargo vet regenerate imports
cargo vet regenerate exemptions
cargo vet regenerate unpublished
cargo vet --locked
'''
install_crate = "cargo-vet"
