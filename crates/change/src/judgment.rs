//! The change loop's judgment leg: plan-time lead reconciliation.
//! Schema-gated kernel in [`project::judgment`]; this crate owns the
//! propose prompt corpus. Unused until decomposition authoring.

#![allow(dead_code, reason = "propose judgment is unused until the decomposition authoring step")]

pub mod propose;
pub mod prose;
