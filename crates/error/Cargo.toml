[package]
name = "emery-error"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Unified error types for the Emery CLI"
categories = ["development-tools"]
keywords = ["emery", "errors"]
readme = "../../README.md"

# Workspace code imports the short crate name; the package publishes as emery-error.
[lib]
name = "error"

[lints]
workspace = true

[dependencies]
serde-saphyr.workspace = true
thiserror.workspace = true
