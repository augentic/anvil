//! Golden + fixture tests for the workflow contract: the merge-engine
//! goldens. Wire-schema accept/reject fixtures live in the schema crate
//! (`crates/schema/tests/wire_fixtures.rs`).

#[path = "goldens/merge_engine.rs"]
mod merge_engine;
