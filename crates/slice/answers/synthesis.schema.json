{
  "$defs": {
    "Assessment": {
      "additionalProperties": false,
      "description": "Judgment-supplied integers (0–[`DIM_MAX`]) for the five dimensions.",
      "properties": {
        "behavioural-breadth": {
          "description": "Behavioural-breadth score.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "context-volume": {
          "description": "Context-volume score.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "coupling": {
          "description": "Coupling score.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "uncertainty": {
          "description": "Uncertainty score.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "verification-surface": {
          "description": "Verification-surface score.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "behavioural-breadth",
        "coupling",
        "uncertainty",
        "context-volume",
        "verification-surface"
      ],
      "type": "object"
    },
    "FocusParent": {
      "additionalProperties": false,
      "description": "One catalog parent named for focused survey.",
      "properties": {
        "lead": {
          "description": "Catalog lead id within that source.",
          "type": "string"
        },
        "source": {
          "description": "Plan source key.",
          "type": "string"
        }
      },
      "required": [
        "source",
        "lead"
      ],
      "type": "object"
    },
    "SynthesisKind": {
      "description": "Synthesis response envelope kind.\n\nSerialises to `proceed` or `boundary-escalation`.",
      "oneOf": [
        {
          "const": "proceed",
          "description": "Promote the synthesized bundle and continue refinement.",
          "type": "string"
        },
        {
          "const": "boundary-escalation",
          "description": "Evidence supports separately acceptable child boundaries; do\nnot promote artifacts.",
          "type": "string"
        }
      ]
    }
  },
  "$id": "https://github.com/augentic/emery/answers/synthesis.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Generated judgment-answer schema — generated from the Rust wire types by slice::answers; do not edit. Validates the schema-gated answer to the slice synthesis judgment: the typed `proceed | boundary-escalation` envelope. `proceed` promotes the change-artifact bundle the agent staged into the lent workspace (RFC-96 D10); `boundary-escalation` names affected terminal pairs and a typed rationale. The deterministic tail validates the staged tree and runs the projection kernel only on `proceed`.",
  "properties": {
    "affected": {
      "description": "Terminal `(source, lead)` pairs — required on\n[`SynthesisKind::BoundaryEscalation`].",
      "items": {
        "$ref": "#/$defs/FocusParent"
      },
      "type": "array"
    },
    "assessment": {
      "$ref": "#/$defs/Assessment",
      "description": "Closed five-dimension complexity assessment."
    },
    "findings": {
      "description": "Advisory agent-reported synthesis notes (evidence gaps\npreserved, divergences resolved, …). Review signals only — the\ndeterministic tail neither parses nor gates them.",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "kind": {
      "$ref": "#/$defs/SynthesisKind",
      "description": "Envelope kind."
    },
    "rationale": {
      "description": "Why the Evidence supports a split — required on\n[`SynthesisKind::BoundaryEscalation`].",
      "type": [
        "string",
        "null"
      ]
    },
    "slice": {
      "description": "Slice name (kebab-case).",
      "type": "string"
    },
    "version": {
      "const": 4,
      "description": "Schema version.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    }
  },
  "required": [
    "version",
    "kind",
    "slice",
    "assessment"
  ],
  "title": "Emery synthesis answer",
  "type": "object"
}
