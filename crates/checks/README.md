# checks

Repo invariants enforced as plain cargo tests: adapter/engine dependency boundary, docs/plugin link integrity, and plugin authoring shape. See [Consistency Checks](../../docs/contributing/checks.md).

```bash
cargo test -p checks
```
