# Per-predicate per-file violation baselines.
#
# Each `[file."<rel-path>"]` table caps the number of violations of each
# predicate for that file. A live count strictly greater than the
# baseline fails CI; missing entries default to 0 (new files start
# clean). Reductions are encouraged; raises require justification in
# the PR.
#
# Predicate definitions live under `scripts/checks/`.
# Loaded by `scripts/checks/_shared.ts::standardsAllowlist`.

[file."plugins/spec/skills/init/SKILL.md"]
argumentHintCoversBodyArguments = 4

[file."plugins/spec/skills/build/SKILL.md"]
argumentHintCoversBodyArguments = 1

[file."plugins/vectis/skills/android-writer/SKILL.md"]
argumentHintCoversBodyArguments = 1

[file."plugins/vectis/skills/template-updater/SKILL.md"]
argumentHintCoversBodyArguments = 1

[file."plugins/omnia/skills/crate-writer/SKILL.md"]
argumentHintCoversBodyArguments = 1

[file."plugins/contract/skills/json-schema/SKILL.md"]
argumentHintCoversBodyArguments = 1

# `sectionLineCount` baselines. Each entry caps the number of H2
# sections that may exceed the 45-line per-section ceiling. Grandfathered
# below the line-count cap tightened from 60 → 45 in F7. New SKILL.md
# files start at 0; entries here ratchet down with each touch.

[file."plugins/spec/skills/define/SKILL.md"]
sectionLineCount = 1

[file."plugins/spec/skills/merge/SKILL.md"]
sectionLineCount = 1

[file."plugins/spec/skills/extract/SKILL.md"]
sectionLineCount = 1

[file."plugins/rt/skills/wiretapper/SKILL.md"]
sectionLineCount = 1

[file."plugins/vectis/skills/test-writer/SKILL.md"]
argumentHintCoversBodyArguments = 3
