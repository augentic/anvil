# Specify.toml is development configuration.
# Runtime configuration lives at .specify/project.yaml.

# :schema https://raw.githubusercontent.com/augentic/specify-cli/main/schemas/authoring/framework.schema.json

# Pin specify-cli dependency:
#   cli = { version = "X.Y.Z" }                       # git tag
#   cli = { git = "<url>", rev|branch|tag = "…" }     # git ref
# 
# Local co-development using Specify.local.toml:
#   cli = { path = "../specify-cli" }
cli = { git = "https://github.com/augentic/specify-cli", branch = "rfc-41" }
