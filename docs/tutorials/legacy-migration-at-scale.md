# Legacy migration at scale

The 1.x legacy-migration walk-through has been retired. In 2.0, legacy code enters the workflow through the `code-typescript` source adapter (and future language siblings). See [Anatomy of an adapter](../explanation/adapter-anatomy.md).
