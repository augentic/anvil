# Cross-repo changes

The 1.x cross-repo walk-through has been retired. The 2.0 rhythm (`/spec:plan` → Gate 1 → `/spec:execute` → `/spec:finalize`) handles single-repo and multi-repo changes uniformly. See [What is Specify?](../orientation/index.md) and the [Registry reference](../reference/registry.md).
