# specify adapter (retired)

The `specify adapter *` verb family retired at 2.0. Source adapters resolve through `specify source resolve <name>`; target adapters through `specify target resolve <value>`. See [Anatomy of an adapter](../../explanation/adapter-anatomy.md).
