# /spec:drop

Discard a slice without merging specs into the baseline.

**Canonical reference.** The authoritative operator surface — synopsis, arguments, the step-by-step critical path, guardrails, closing hints, and error modes — is the [`/spec:drop` skill body](../../../plugins/spec/skills/drop/SKILL.md). This page is a navigation stub and carries no operator steps, so the two surfaces cannot drift.

## See also

- [/spec:merge](merge.md) — the alternative to dropping
- [Lifecycle](../lifecycle.md) — the dropped state
