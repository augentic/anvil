# /spec:build

Implement tasks from a refined slice by driving the two-phase `specify slice build` verb and running the target adapter's build brief.

**Canonical reference.** The authoritative operator surface — synopsis, arguments, the step-by-step critical path, guardrails, closing hints, and error modes — is the [`/spec:build` skill body](../../../plugins/spec/skills/build/SKILL.md). This page is a navigation stub and carries no operator steps, so the two surfaces cannot drift.

## See also

- [/spec:refine](refine.md) — generate artifacts before building
- [/spec:merge](merge.md) — next step after all tasks complete
- [Drive a slice manually](../../how-to/drive-slice-manually.md) — when execute parks on build
- [Artifact format](../artifact-format.md) — skill directive tag syntax
- [specify slice build](../cli/slice.md#specify-slice-build) — the CLI verb the skill drives
