name: CI

on:
  push:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_INCREMENTAL: 0
  CARGO_NET_RETRY: 10
  CARGO_TERM_COLOR: always
  RUSTUP_MAX_RETRIES: 10

jobs:
  checks:
    name: Documentation and consistency checks
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Choose specify-cli ref
        id: specify-cli-ref
        shell: bash
        run: |
          branch="${GITHUB_HEAD_REF:-${GITHUB_REF_NAME}}"
          if git ls-remote --exit-code --heads https://github.com/augentic/specify-cli.git "$branch" >/dev/null 2>&1; then
            echo "ref=$branch" >> "$GITHUB_OUTPUT"
          else
            echo "ref=main" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/checkout@v6
        with:
          repository: augentic/specify-cli
          path: specify-cli
          ref: ${{ steps.specify-cli-ref.outputs.ref }}
          fetch-depth: 1
          sparse-checkout: schemas
          sparse-checkout-cone-mode: false

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: tooling
          save-if: ${{ github.ref == 'refs/heads/main' }}
          cache-on-failure: true
          
      - name: Verify framework
        run: |
          cargo test --locked --manifest-path tooling/Cargo.toml
          cargo run --locked --manifest-path tooling/Cargo.toml -- check
        env:
          SPECIFY_CLI_DIR: specify-cli
