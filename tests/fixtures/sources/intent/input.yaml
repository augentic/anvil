# Inputs the CLI hands the `intent` source adapter for the degenerate N=1 path.
# `enumerate` receives `source` + `slice-name`; `extract` receives `source` +
# `candidate` (which equals `slice-name` under the bare-string shorthand for
# `Slice.sources`).
slice-name: add-search-filter
source-key: intent
source:
  adapter: intent
  value: "Add a search filter to the user list."
candidate: add-search-filter
