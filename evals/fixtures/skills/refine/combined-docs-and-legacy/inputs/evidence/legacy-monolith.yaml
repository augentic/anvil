authority: behaviour
lead: user-registration
claims:
  - kind: excerpt
    id: users.register.email-validation
    path: src/users/register.ts#L12-L34
    excerpt: "Handler validates email against an RFC-5322 regex; returns 400 with { error: \"invalid-email\" } on failure, otherwise inserts the user and returns 201 with the persisted record."
  - kind: type
    path: src/users/repository.ts#L1-L4
    signature: "interface User { id: string; email: string; createdAt: Date }"
  - kind: call
    path: src/users/register.ts#L31
    callee: "src/users/repository.ts:insertUser"
