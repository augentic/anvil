{
  "version": 1,
  "kind": "response",
  "slice": "@@SLICE@@",
  "model": {
    "requirements": [
      {
        "title": "@@STATEMENT@@",
        "domain": "@@SLICE@@",
        "claims": [{ "source": "intent", "id": "@@SLICE@@", "kind": "intent" }],
        "agreement": "agreed",
        "statement": "@@STATEMENT@@",
        "scenarios": ["WHEN the service handles @@SLICE@@ THEN behaviour matches the intent"]
      }
    ],
    "tasks": [
      { "id": "TASK-001", "text": "Implement @@SLICE@@ library surface.", "satisfies": ["REQ-001"] },
      { "id": "TASK-002", "text": "Run cargo test to verify behaviour.", "satisfies": ["REQ-001"] }
    ]
  },
  "artifacts": {
    "proposal": "# @@SLICE@@\n\n## Domains\n\n- @@SLICE@@ — eval slice crate.\n",
    "design": "# Design\n\nMinimal serde library for @@SLICE@@.\n",
    "tasks": "# Tasks\n\n## 1. Implement\n\n- [ ] 1.1 Implement library surface.\n- [ ] 1.2 Run cargo test.\n",
    "specs": [
      {
        "domain": "@@SLICE@@",
        "content": "## Overview\n\n@@STATEMENT@@\n\n#### Scenario: @@SLICE@@ behaviour\n\nWHEN the service handles @@SLICE@@ THEN behaviour matches the intent\n"
      }
    ]
  }
}
