.env
.DS_Store
docs/book/

# Repo-local built binaries (scripts/specify.rs installs the resolved CLI here)
.cli/

# Gitignored local overlay: cli = { path = "../specify-cli" } for CLI co-development
Specify.local.toml

# # In-repo checkout of the specify-cli SOURCE for incremental, cacheable path-mode
# # builds in CI (.github/workflows/ci.yaml); kept out of the framework lint walk.
# /specify-cli/

# Machine-local WASI tool sidecar generated by scripts/use-local-dev.rs
adapters/**/tools.yaml

# Stable, browsable eval sandbox (replaces ad-hoc mktemp temp roots)
evals/.sandbox/