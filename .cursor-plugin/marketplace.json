{
  "name": "augentic",
  "owner": {
    "name": "augentic",
    "email": "info@augentic.io"
  },
  "metadata": {
    "description": "Spec-driven code generation plugins for Augentic.",
    "version": "0.26.0",
    "pluginRoot": "plugins"
  },
  "plugins": [
    {
      "name": "rt",
      "source": "rt",
      "description": "Skills to assist in the migration of TypeScript projects to Omnia Rust wasm32-wasip2 crates."
    },
    {
      "name": "client",
      "source": "client",
      "description": "Skills to generate client-facing deliverables — Statements of Work, proposals, pricing summaries, and similar artefacts — from Specify artifacts."
    },
    {
      "name": "spec",
      "source": "spec",
      "description": "Skills for the Specify 2.0 workflow: `/spec:init` scaffolds a project, `/spec:plan` authors the change-level plan, `/spec:execute` drives the per-slice loop, and the per-slice breakouts (`/spec:refine`, `/spec:build`, `/spec:merge`, `/spec:drop`) plus `/spec:finalize` round out the operator surface."
    }
  ]
}
