{
  "$schema": "../cli/schemas/authoring/marketplace.schema.json",
  "name": "augentic",
  "owner": {
    "name": "augentic",
    "email": "info@augentic.io"
  },
  "metadata": {
    "description": "Spec-driven code generation plugins for Augentic.",
    "version": "0.27.0",
    "pluginRoot": "plugins"
  },
  "plugins": [
    {
      "name": "capture",
      "source": "capture",
      "description": "Skills to capture runtime fixtures from legacy TypeScript services for Specify migration workflows."
    },
    {
      "name": "client",
      "source": "client",
      "description": "Experimental: skills to generate client-facing deliverables — Statements of Work, proposals, pricing summaries, and similar artefacts — from Specify artifacts. One skill, no eval scenario; status under review."
    },
    {
      "name": "spec",
      "source": "spec",
      "description": "Skills for the Specify workflow: `/spec:init` scaffolds a project, `/spec:plan` authors the change-level plan, `/spec:execute` drives the per-slice loop, and the per-slice breakouts (`/spec:refine`, `/spec:build`, `/spec:merge`, `/spec:drop`) plus `/spec:finalize` round out the operator surface."
    }
  ]
}
