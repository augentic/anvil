# Sibling token manifest for the task-list-two-screen fixture. Present so
# the references in `expected.layout.yaml` (gap / padding / color / size
# tokens) resolve cleanly under `vectis validate composition` —
# the contract bars inferers from inventing token names, so the fixture
# only emits names that this manifest defines.

version: 1

provenance:
  sources:
    - kind: manual
      captured_at: "2026-05-06T07:55:00Z"

spacing:
  xs: 4
  sm: 8
  md: 12
  lg: 16

colors:
  on-surface-variant:
    light: "#454545"
    dark: "#B0B0B0"
  error:
    light: "#B3261E"
    dark: "#F2B8B5"
