# Code & Skill Review — Subtraction Pass (second sweep)

## Summary

Top three by sort key: (F1) delete two journal-unit tests fully subsumed by `tests/journal.rs::agent_emit_helper_writes_one_event_per_jsonl_line` (−67 LOC, −2 oracles); (T1) collapse the unused `NameOwner` enum in `discovery/document.rs` whose own doc admits the discriminator is never read (−≈21 LOC, −1 enum, −1 impl); (T2) delete three pre-1.0 documentation tombstones (`docs/reference/cli/change.md`, `docs/reference/slice-skills/define.md`, `docs/reference/slice-skills/extract.md`) and the one remaining misleading caller in `docs/reference/cli/slice.md:248` (−≈12 LOC, −3 stub files, −1 wrong link). Total ΔLOC if every finding lands: ≈−138. Primary non-LOC axes: duplicate tests, hand-rolled types, stale docs. **Verified defects: none qualified** — `make checks` (specify) and `cargo make check` (specify-cli) both pass; clippy is clean; every production `unwrap`/`expect` carries a documented invariant. Net ΔLOC from defect-only findings: 0. Highest remediation risk: F1 — the two unit tests pin `append` (not `append_batch`) wire shape; integration coverage uses the same `append` code path, so deletion is safe, but anyone unfamiliar with the journal stack will reach for them first when adding a new event variant and might re-introduce one.

## Reconnaissance

- `tokei` (combined): 156 231 lines / 1090 files — specify 84 806 / 606; specify-cli 71 425 / 484. Rust 47 050 (cli) + ≈9 608 (specify Markdown-embedded) lines.
- `cargo tree --duplicates` (specify-cli): duplicates are entirely transitive from `wasm-pkg-client` (`base64 0.21/0.22`, `pbjson`, `warg-*`, `oci-client`, `reqwest 0.12/0.13`); frozen for the pass.
- `rg -c '^#\[test\]' crates/ src/ tests/` (specify-cli): 39 files / **663** total `#[test]` functions. Heaviest: `tests/plan_orchestrate.rs` (77), `crates/domain/tests/registry.rs` (50), `tests/slice.rs` (44), `crates/domain/tests/workspace.rs` (38), `crates/domain/src/change/plan/core/validate/tests.rs` (28), `tests/context.rs` (28), `crates/domain/tests/finalize.rs` (28), `tests/registry.rs` (22).
- `rg --files -g '**/mod.rs'` (specify-cli): **3** hits — all test shims (`tests/common/mod.rs`, `crates/domain/tests/common/mod.rs`, `wasi-tools/vectis/tests/engine_support/mod.rs`).
- `wc -l docs/standards/*.md AGENTS.md` (combined): **1035** total — specify 398, specify-cli 637.
- Files > 500 lines under `crates/` and `src/` (specify-cli, production source only): `src/commands/plan/create.rs` 1080; `crates/domain/src/discovery/document.rs` 943; `crates/domain/src/slice/fusion.rs` 906; `crates/domain/src/change/plan/core/model.rs` 827; `crates/domain/src/journal.rs` 701; `crates/domain/src/spec/provenance.rs` 599; `crates/domain/src/change/plan/core/validate/tests.rs` 587; `crates/tool/src/validate.rs` 520; `crates/domain/src/adapter/cache/io.rs` 508; `crates/domain/src/adapter/cache.rs` 503.
- `make checks` (specify): **All checks passed** (one prior `rfc-27-plan` link pre-existing failure cleared between sweeps).
- `cargo make check` (specify-cli): **940 tests pass, 2 skipped** (148s); fmt + lint + test + test-docs all green.
- `cargo clippy --workspace --all-targets` (specify-cli): clean.
- Production-only `unwrap`/`expect` (excluding `#[cfg(test)]`, `tests/`, `*/tests.rs`, `wasi-tools/`): **28** call sites in 21 files; every one carries a documented invariant — regex literals, post-`find` lookups, schema-validated serde, `clamp(0,255)` casts. No qualifying operator-panic defect.
- `rg -c 'panic!|unreachable!' --glob '!**/tests/**'` (specify-cli, production): 1 — `unreachable!("unexpected EntryBody action: {other}")` at `src/commands/plan/create.rs:1078`, guarded by exhaustive caller match.

## Structural Findings

### F1 — Delete two journal.rs unit tests subsumed by `tests/journal.rs`

- **Evidence**:
  - `wc -l crates/domain/src/journal.rs` → **701** (≈362 production, ≈339 in `#[cfg(test)] mod tests`).
  - `crates/domain/src/journal.rs:450–475` — `appending_two_events_writes_two_lines_in_order` calls `append` twice (`SliceExtractCompleted` + `SliceSynthesisConflict`), asserts two lines containing those event ids.
  - `crates/domain/src/journal.rs:477–518` — `all_synthesis_tag_variants_serialise` loops `append` over the three `SliceSynthesis*` variants, asserts the `event` and `requirement-id:` substrings.
  - `tests/journal.rs:343–407` — `agent_emit_helper_writes_one_event_per_jsonl_line` calls `append` five times across `PlanProposeDivergence`, `SliceExtractCompleted`, and all three `SliceSynthesis*` variants; asserts five lines, JSON object shape, and a golden snapshot at `tests/fixtures/journal/agent-emit-helper.json`. The unit-test surface (the `append` code path, the three synthesis ids, multi-line append ordering) is fully covered.
  - `tests/journal.rs:409–421` — `divergence_kebab_case_round_trip` plus the unit-only `no_snake_case_fields_or_values_leak_to_wire` (`journal.rs:662–700`) already pin kebab-case wire enforcement.
- **Action**:
  1. Delete `journal.rs:450–475` (`appending_two_events_writes_two_lines_in_order`).
  2. Delete `journal.rs:477–518` (`all_synthesis_tag_variants_serialise`).
  3. Leave the wire-shape pins (`slice_extract_cache_*_wire_shape`, `slice_fusion_written_wire_shape`, `slice_fixture_replay_completed_wire_shape`, `plan_amend_authority_override_*`, `no_snake_case_fields_or_values_leak_to_wire`, `append_batch_*`, `append_creates_specify_dir_when_missing`) untouched — they pin shapes the integration suite does not cover.
- **Quality delta**: −≈67 LOC, −2 duplicate oracles, −0 panic surface.
- **Net LOC**: `crates/domain/src/journal.rs` 701 → ≈634.
- **Done when**: `rg 'appending_two_events_writes_two_lines_in_order|all_synthesis_tag_variants_serialise' crates/` returns **0** and `cargo make check` stays green.
- **Rule?**: no.
- **Counter-argument**: "Unit tests run against the in-process `append` whereas the integration test goes through `specify` only for `agent_emit_helper_writes_one_event_per_jsonl_line` which itself calls `append` directly via the public helper" — loses because the integration test imports `specify_domain::journal::{self, Event, EventKind}` and drives `append` exactly as the unit test does (`tests/journal.rs:392`). Same code path; smaller unit set.
- **Depends on**: none.

## One-touch tidies

1. **Collapse `NameOwner` enum in `discovery/document.rs`** — `crates/domain/src/discovery/document.rs:360–380` admits in its own doc "The variant is kept even though the report only emits bearing ids — the discriminator keeps future per-variant findings cheap to bolt on." `rg 'NameOwner::(Id|Alias) ' crates/` confirms no consumer ever discriminates: both arms feed `bearing_id()`, which is called once. Change `BTreeMap<String, Vec<NameOwner>>` (line 179) to `BTreeMap<String, Vec<String>>`, push `candidate.id.clone()` directly at lines 184 and 189, swap `.iter().map(NameOwner::bearing_id)` for `.iter().cloned()` at line 198, delete the comment block and the enum + impl (lines 360–380). Δ: ≈−21 LOC, −1 enum, −1 impl. Counter: "Future divergence detection might want the discriminator" — loses because three sweeps in, no caller has asked for it, and `git log` shows the enum landed unused.

2. **Delete pre-1.0 documentation tombstones** — `docs/reference/cli/change.md` (3 lines, retired `specify change` redirect), `docs/reference/slice-skills/define.md` (3 lines, retired `/spec:define`), `docs/reference/slice-skills/extract.md` (3 lines, retired `/spec:extract`). `rg -l 'cli/change\.md|slice-skills/define|slice-skills/extract'` → only `docs/reference/cli/slice.md:248` (a misleading link labeled `/spec:refine` that points at `define.md`) and the `rfcs/archive/` corpus (skipped by the link checker). Action: `rm` the three stubs, then in `docs/reference/cli/slice.md:248` either drop the bullet or rewrite to `[/spec:refine](../slice-skills/index.md) -- per-slice refine breakout`. Δ: ≈−12 LOC, −3 stub files, −1 wrong link label. Counter: "External bookmarks may still hit the old URLs" — loses because pre-1.0 has no compatibility surface and `make checks` already skips redirects.

3. **Delete `apply_authority_override_assigns` + `set_events` in `src/commands/plan/create.rs`** — `apply_authority_override_assigns` (lines 321–342) is structurally the per-call-site equivalent of `dedup_sets` (404–415) + `apply_sets` (460–464) with an inline unknown-slice gate; `set_events` (349–380) duplicates `emit_override_events` (497+) for the same `Set` variant. The `create` handler at `src/commands/plan/create.rs:706–748` runs the pair instead of routing through `mutate_authority_overrides`. Refactor: in `create`, after `apply_divergence_likely`, call `dedup_sets(&override_assigns)`, run the orphan check, call `apply_sets`, then build events through the existing `emit_override_events` (or its `Set`-only branch) — both deduped. Δ: ≈−45 LOC, −2 helpers, −1 duplicate dispatch path. **Borderline tidy/structural** — only list if execute appetite exists; the two helpers are coherent in isolation. Counter: "The `create` path needs only `Set` events, never `Clear`/`ClearAll`, so the simpler helpers are fit-for-purpose" — partially survives because the orphan + unknown-slice gates already live in shared code; the dedup + set + emit triple still collapses.

4. **Delete `journal::append` wrapper** — `crates/domain/src/journal.rs:314–316` is `append_batch(layout, std::slice::from_ref(event))`. Production callers: `src/commands/source/cache.rs:99`, `src/commands/plan/lifecycle.rs:169` (2 sites). Tests: `tests/journal.rs:392` (rewrite once). Inline `append_batch(layout, std::slice::from_ref(&event))?` at each call. Δ: ≈−10 LOC (wrapper + 4 lines of `# Errors` / `# Panics` docs), call-site delta near zero. Counter: "Single-event ergonomics" — loses because the slice form reads identically.

5. **Delete `SliceAuthorityOverride::from_pairs` and `SliceAuthorityOverride::resolve`** — `crates/domain/src/change/plan/core/model.rs:236–256`. `rg 'SliceAuthorityOverride::(from_pairs|resolve)\(' --glob '!**/tests*'` → **0** production callers. Three test sites (`crates/domain/src/change/plan/core/validate/tests.rs:484, 524, 545`) build via `from_pairs`; two (`model.rs:765, 766`) call `resolve`. Replace `from_pairs([...])` with `SliceAuthorityOverride { by_kind: BTreeMap::from([...]) }` (validate/tests.rs already imports `BTreeMap`); replace `.resolve(kind)` with `.by_kind.get(&kind).map(String::as_str)`. Δ: ≈−16 LOC for the helpers, ≈+8 LOC at the five test call sites — net ≈−8 LOC. **Borderline; only run if touching the file anyway.** Counter: "Test ergonomics" — loses by the no-test-only-public-helpers rule once the call count is this small.

6. **Delete `AuthorityOverrides::from_pairs` and `is_empty`** — `crates/domain/src/evidence/authority.rs:152–175`. Production keeps `AuthorityOverrides::default()` (10 sites) and `AuthorityOverrides::resolve` (310, 338). `from_pairs` is called from `authority.rs:206, 219, 394, 467` (all `mod tests`); `is_empty` from `authority.rs:259` (`mod tests`). Replace `from_pairs([...])` with `AuthorityOverrides { by_kind: BTreeMap::from([...]) }` and `is_empty()` with `.by_kind.is_empty()`. Δ: ≈−18 LOC for the helpers, ≈+10 at the five test sites — net ≈−8 LOC. Same borderline calculus as T5.

7. **Drop the stale "Phase 1/2" `journal::append` mirror comment** — `crates/domain/src/journal.rs:307–313` doc block on `append` echoes `to_value(entry).expect("plan Entry serialises as JSON")` from `src/commands/plan/create.rs`. The mirrored quote is wrong (the actual call site uses `to_value(created).expect("plan Entry serialises as JSON")`). Replace the seven-line `# Panics` block with one line: `Panics only if [`serde_json::to_string`] fails for a closed-derive [`Event`]; unreachable in normal operation.` Δ: ≈−5 LOC, −1 misleading copy-paste. (Comment edit is allowed by the brief because the comment "actively misleads" — it points at a non-existent call site and a non-matching expect message.)

## Dropped findings

- **Defect-only finding pool** — every production `unwrap`/`expect` audited carries a documented invariant ("we just pushed", "regex literal", "schema-validated", "clamp 0..=255"). No qualifying CI / wire-contract / panic-surface / skill-integrity defect remains after the first sweep. Net positive defect-only ΔLOC: 0 (well under the +30 cap).
- **Collapse `RequirementTag` into `RequirementStatus`** — `crates/domain/src/spec/provenance.rs:97–137`. Variants are a strict subset (`Unknown`, `Conflict`, `Divergence`) and the mapping in `expected_status()` is identity. But the `tag: Option<RequirementTag>` field semantically encodes "tag absent" vs "tag present (and not `Agreed`)", and the validator at :443 (`else if status != RequirementStatus::Agreed`) flags missing tags; collapsing into `Option<RequirementStatus>` would force every caller that destructures `tag` to re-check `Some(_) && _ != Agreed`. Net axes neutral. Kept.
- **Inline `parse_slice_kind_assign_args` + `parse_slice_kind_args` into one shared helper** — `src/commands/plan/create.rs:258–314`. Bodies differ only in the third field (`source_key` present vs absent). Sharing requires either a generic over the chunk shape or an `Option<String>` field on a merged type; both add LOC or introduce a fourth helper. Kept.
- **Delete `Patch<T>` three-way enum and the four-arm `match` in `amend`** — `src/commands/plan/create.rs:934–948` repeats `None → Keep, Some("") → Clear, Some(_) → Set` three times across `project`/`target`/`description`. A `fn patch_from(s: Option<String>) -> Patch<String>` helper would save ≈8 LOC but introduces a third helper near a closure that already crosses the 1080-line file. Net not worth the maintenance edge. Kept.
- **Delete `.cursor/rules/project.mdc`** — 250-line `alwaysApply: true` rule that materially overlaps the workspace-loaded `AGENTS.md` (vocabulary, workflow, specialist-skill summary). But the Cursor-only `.mdc` also carries unique sections (Authority Hierarchy precedence, Artifact Boundaries detail, Repository Layout tree, Troubleshooting) that AGENTS.md does not. A subtraction would require lifting those into AGENTS.md (net ≈−150) or accepting Cursor users lose context. Architecturally non-trivial; the LOC gain alone does not pay for the audience-split discussion. Kept.
- **Move `discovery/document.rs` inline tests to `tests/`** — would add a file; forbidden under "No new files unless net-negative". Kept.
- **Promote ad-hoc `Error::Diag { code: "…" }` codes to typed variants** — `rg 'code: ?"' crates/ src/` shows hundreds of unique discriminants; promotion adds enum + `Exit::from` rows for net `+` LOC and no axis movement. Kept.
- **Merge `journal.rs` `appending_two_events_writes_two_lines_in_order` into the existing `append_batch_writes_every_event_in_order_in_one_call`** — different code paths (`append` vs `append_batch`); F1 deletes the redundant one outright instead.
- **Collapse `change/plan/core/` into a single file** — eight modules ≈2 800 LOC combined; violates the per-file size norm in `docs/standards/architecture.md`. Kept.

## Post-mortem

One line per applied finding: actual ΔLOC vs predicted, did the "done when" assertion flip cleanly, did anything regress.

(Pending; this section is filled in only after a finding lands. The prior pass's post-mortem covered F1–F5 / T1–T8 and is captured in the previous review revision.)

- **F1** — actual −70 vs predicted −67 (+4%); deleted `appending_two_events_writes_two_lines_in_order` + `all_synthesis_tag_variants_serialise`; `journal.rs` 701→631; `rg` returns 0 hits; `cargo make check` 938 pass / 2 skipped (was 940/2), exactly matching the two deletions; no helpers orphaned; no regressions.
- **T1** — actual −22 vs predicted −21 (+5%); collapsed `NameOwner` to bare `String`; `rg 'NameOwner' crates/ src/` 0 hits; `cargo make check` green; clippy quirk under `-Dwarnings` forced `let mut bearing: Vec<String> = owners` (move) instead of `.iter().cloned().collect()` or `.to_vec()`; no regressions.
- **T2** — actual −14 vs predicted −12 (+17%); deleted three pre-1.0 doc tombstones; deleting `cli/change.md` surfaced 8 additional broken links across status/workspace/plan/index/registry/slice docs (not listed in the finding but neighbouring callers); retargeted them at `plan.md` / `slice.md`; `make checks` green; no regressions.
- **T3** — actual −56 vs predicted −45 (+24%); collapsed `apply_authority_override_assigns` + `set_events`; create handler now calls `mutate_authority_overrides` with empty clears; events byte-identical, `plan-authority-override-unknown-slice` error code preserved (only summary/detail wording differed, no test/golden pinned the strings); moved an orphaned section doc-comment to its proper home; `cargo make check` 938 pass; no regressions.
- **T7** — actual −6 vs predicted −5 (+20%); replaced misleading `# Panics` block on `journal::append` with single accurate line; `# Errors` block preserved; ran before T4 to avoid being mooted by the wrapper deletion; `cargo make check` 938 pass; no regressions.
- **T4** — actual −12 vs predicted −10 (+20%); deleted `journal::append` wrapper, all callers routed through `append_batch(layout, std::slice::from_ref(&event))`; review undercounted production sites (3 not 2 — missed `crates/domain/src/slice/actions/transition.rs`); 8 internal test usages and 1 external test site rewritten; `cargo make check` green; no regressions.
