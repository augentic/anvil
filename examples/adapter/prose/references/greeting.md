# Greeting source

The mock source documents one behaviour: GET /greeting returns the static string 'hello'. Requirement ids stay `greeting.behaviour`. A criterion id extends that id (`greeting.behaviour.body`) when the bound tree states acceptance.
