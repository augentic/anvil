# Tiny hypothetical documentation input

A two-procedure ops runbook used only to illustrate the `/change:analyze` output shape. The documents referenced below are not present — this fixture pins the *expected output* for a notional input, not a runnable tree.

Candidates the scaffold example summarises:

- `drain-backpressure-queue` — drain the backpressure queue when ingest depth stays above capacity. Declared at `ops-runbook.md#drain-the-backpressure-queue`.
- `rotate-upstream-ingest-key` — rotate the upstream ingest key when the secret provider rolls it. Declared at `ops-runbook.md#rotate-the-upstream-ingest-key`.

Real documentation extraction fixtures land with the per-capability brief fixtures.
