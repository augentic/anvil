# traffic-ingest legacy service (stub)

Illustrative legacy-code input. Two handlers (`submit`, `replay`) in `src/ingest.rs` — just enough structure for `/change:analyze legacy-code` to surface two adapters (`ingest-submit`, `ingest-replay`) with kafka + redis as inferred external deps.
