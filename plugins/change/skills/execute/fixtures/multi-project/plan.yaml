name: platform-v2
sources:
  monolith: /path/to/legacy-codebase
changes:
  - name: ingest-pipeline
    project: traffic
    sources: [monolith]
    status: pending
    description: "Extract the Kafka ingestion pipeline into a standalone adapter."
  - name: operator-dashboard
    project: command-centre
    status: pending
    depends-on: [ingest-pipeline]
    description: "Build the operator alerting dashboard."
  - name: traffic-api
    project: traffic
    status: pending
    depends-on: [ingest-pipeline, operator-dashboard]
    description: "Expose the traffic state as a REST API."
