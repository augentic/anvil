# traffic-ingest legacy service (stub)

Illustrative legacy-code input. Two handlers (`submit`, `replay`) in `src/ingest.rs` — just enough structure for `/spec:analyze legacy-code` to surface two capabilities (`ingest-submit`, `ingest-replay`) with kafka + redis as inferred external deps.
