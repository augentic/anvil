---
description: Specify plugin context -- Specify workflow orchestration for defining, building, merging, and dropping slices
alwaysApply: true
---

# Specify Plugin - Specify Workflow

## Overview

This plugin provides the core Specify workflow skills: define a slice, build (validate and implement) a slice, merge a completed slice, drop a discarded slice, and initialize new projects. These skills orchestrate the human-driven Specify lifecycle that the specialist plugins (Omnia, RT, Client) build on.

**Purpose**: Drive the define -> build -> merge workflow, with drop for cleanup.

## Adapter Structure

Adapters define the artifact graph, briefs, and heading conventions. Each adapter directory contains:

```text
adapters/<name>/
├── adapter.yaml
└── briefs/
```

`adapter.yaml` defines the pipeline phases — detailed guidance lives in brief files with YAML frontmatter.

## Slice Lifecycle

Slices track their status in `.metadata.yaml`:

```text
defining -> defined -> building -> complete -> merged
    \          \          \
     `-------> `-------> `-------> dropped
```

- **defining**: Artifacts are being generated (define in progress)
- **defined**: All artifacts created, ready for implementation
- **building**: Tasks are being implemented (artifacts validated on entry)
- **complete**: All tasks done, ready to merge
- **merged**: Specs merged into baseline, slice moved to archive
- **dropped**: Slice moved to archive without merging specs into the baseline

## Workflow

```text
/spec:init (once per project)

/spec:define  ->  /spec:build  ->  /spec:merge
                       ^
                       |
                  /spec:drop    (discard instead of merge)
                  /spec:extract (extract artifacts from existing source code)
```

- **init**: One-time project setup -- creates `.specify/` directory structure and `project.yaml`
- **define**: Create a slice directory and generate artifacts in dependency order. Supports regenerating a single artifact with `/spec:define <name> <blueprint-id>`.
- **build**: Validate artifacts against `adapter.yaml` rules and cross-blueprint consistency, then implement tasks from the slice, marking them complete as you go
- **merge**: Preview merge operations, finalize a completed slice, merge delta specs into baseline
- **drop**: Discard an active slice, set `status: dropped`, and move it to archive without merging specs
- **extract**: Extract Specify artifacts (specs + design.md) from existing source code

## Troubleshooting

### Project Not Initialized

- Run `/spec:init` in the project root to create the `.specify/` directory structure
- Verify with `specify status`

### Change Not Found

- Run `specify status` to see active slices
- Check the slice name matches (kebab-case)

### Artifacts Incomplete

- Run `specify slice status <name>` to check artifact status
- Use `/spec:define <name> <blueprint-id>` to regenerate a single artifact
- Use `/spec:define` to create a new change with all artifacts

## Related Documentation

- See project rule (`.cursor/rules/project.mdc`) for overall project context
- [Init SKILL.md](skills/init/SKILL.md) - Initialize Specify
- [Define SKILL.md](skills/define/SKILL.md) - Create changes with artifacts
- [Build SKILL.md](skills/build/SKILL.md) - Validate artifacts and implement change tasks
- [Merge SKILL.md](skills/merge/SKILL.md) - Finalize completed changes
- [Drop SKILL.md](skills/drop/SKILL.md) - Discard a slice without merging specs
- [Extract SKILL.md](skills/extract/SKILL.md) - Extract artifacts from existing source code
- [Specify Artifact Specification](../references/specify.md) - Artifact format details

## Specification

Do NOT use `openspec/`, `temp/`, or any other directory convention from other plugins or skills.