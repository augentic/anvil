name: combined-initiative

sources:
  monolith: /path/to/legacy-codebase

changes:
  - name: user-registration
    sources: [monolith]
    status: done

  - name: registration-hardening
    sources: [monolith]
    description: >
      Tighten email-parser validation; same legacy source as the
      original extraction, delta-targeting the merged
      user-registration baseline.
    status: pending
