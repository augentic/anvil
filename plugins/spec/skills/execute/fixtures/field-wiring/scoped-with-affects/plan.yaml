name: scoped-with-affects-initiative

sources:
  monolith: ./legacy/monolith

changes:
  - name: user-registration
    sources: [monolith]
    status: done

  - name: email-verification
    sources: [monolith]
    status: done

  - name: extract-shared-validation
    sources: [monolith]
    affects: [user-registration, email-verification]
    scope:
      monolith:
        include:
          - src/common/validation/**
    description: >
      Lift the shared validation helpers out of the two already-
      merged baselines into their own slice; delta-target both.
    status: pending
