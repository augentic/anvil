name: scoped-initiative

sources:
  monolith: ./legacy

changes:
  - name: ingest-pipeline
    sources: [monolith]
    scope:
      monolith:
        include:
          - src/ingest/**
          - src/kafka/**
        exclude:
          - src/ingest/_deprecated/**
    description: >
      Extract the ingest pipeline and its Kafka wiring from the
      legacy monolith; skip the deprecated sub-tree.
    status: pending
