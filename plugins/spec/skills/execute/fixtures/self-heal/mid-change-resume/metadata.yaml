# Snapshot of .specify/changes/product-catalog/.metadata.yaml as
# self-heal reads it. The prior /spec:execute run finished the
# on-disk work of /spec:define (LifecycleStatus advanced to `defined`
# via `specify change transition`) but crashed BEFORE any phase
# stamped an `outcome` field on this file — either the crash caught
# /spec:define mid-flight just after its lifecycle transition but
# before `specify change phase-outcome`, or it caught the driver in
# the window between /spec:define returning and /spec:build starting.
# Either way the concrete on-disk shape is the same: non-terminal
# `LifecycleStatus`, no `outcome` field.
#
# Per RFC-2 §"Plan Mutation and Crash Safety", this is an "active
# change dir with no terminal outcome yet" — self-heal routes this
# to mid-change resume (not to a halt). The resumption target is
# /spec:build (LifecycleStatus=defined → invoke /spec:build).
#
# The recovery journal entry is appended BEFORE /spec:build is
# invoked so the audit log records that the driver picked up the
# change mid-run (not that it started fresh). No plan transition is
# applied — the entry stays `in-progress` while /spec:build runs and
# the normal step-9 phase-outcome read wraps up the run.

name: product-catalog
status: defined
schema: omnia
created-at: 2026-04-19T14:00:00Z
defined-at: 2026-04-19T14:11:43Z
