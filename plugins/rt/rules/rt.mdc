---
description: RT plugin context -- TypeScript to Rust WASM migration patterns and troubleshooting
alwaysApply: true
---

# RT Plugin - TypeScript to Rust WASM Migration

## Overview

This plugin migrates existing TypeScript services to production-grade Rust WASM components targeting the Omnia SDK and wasmtime runtime.

**Purpose**: Automate large-scale migrations from TypeScript to Rust WASM with high fidelity and reproducibility.

## Plugin Architecture

```
/plugins/rt/
├── .cursor-plugin/
│   └── plugin.json      # Plugin manifest
├── rules/
│   └── rt.mdc            # This file
└── skills/
    ├── wiretapper/        # Add wiretap to legacy repo for fixture capture
    └── replay-writer/     # Add regression tests from captured fixtures
```

Note: Source code analysis (formerly `code-analyzer`) has moved to `/spec:extract` in the spec plugin. Repository cloning is no longer a dedicated skill — both callers (`/spec:analyze` and `/rt:wiretapper`) inline a guarded `git clone` snippet directly; see the *Cloning a source tree* subsection in [`plugins/spec/skills/analyze/SKILL.md`](../../spec/skills/analyze/SKILL.md) (or [`plugins/rt/skills/wiretapper/SKILL.md`](../skills/wiretapper/SKILL.md) for legacy-repo bootstrap).

Each skill has its own SKILL.md with full details. After the legacy repo is cloned (via the inlined snippet), **wiretapper** runs in parallel with **analyze** (wiretapper adds `src/wiretap/` to the legacy repo and wires the app entrypoint so that with `WIRETAP_ENABLED=true` the app writes `{app-name}.wiretap.json`). The RT plugin provides the analysis and fixture-capture side; code generation skills (crate-writer, guest-writer, test-writer, code-reviewer) live in the Omnia plugin.

## TypeScript-Specific Patterns

### What the Analysis Layer Captures (Specify artifacts)

- **Async/await patterns**: Documented as concurrent execution patterns
- **Type definitions**: TypeScript interfaces → domain model types
- **REST API handlers**: Express routes → API contracts and business logic blocks
- **Environment variables**: process.env.X → configuration dependencies
- **HTTP requests**: axios/fetch → outbound HTTP external service dependencies
- **Message publishing**: Kafka/RabbitMQ → message publishing capabilities
- **Data access**: Azure Table Storage, Redis, SQL, blob storage, document databases → external service dependencies with type classification

The `/spec:extract` skill produces platform-agnostic Specify artifacts (specs + design.md). The crate-writer skill then maps artifact capabilities to Omnia SDK provider traits (e.g., outbound HTTP → `HttpRequest`, configuration → `Config`).

### Known Challenges

- **Dynamic typing**: TypeScript's `any` → [unknown] tag in artifacts
- **Complex OOP**: Deep inheritance hierarchies need flattening
- **Decorators**: Must manually map to Rust patterns
- **Metaprogramming**: Reflection/proxies can't be directly translated
- **DOM APIs**: Browser-specific code must be redesigned for WASM

## Troubleshooting

### TypeScript Analysis Fails

- Is source valid TypeScript? (run `tsc --noEmit`)
- Are dependencies installed? (`npm install`)
- Is tsconfig.json present?

### Artifacts Missing Business Logic

- Are functions exported? (the extract skill skips internal functions)
- Is logic in accessible files? (check module imports)
- Are there dynamically loaded modules?

### TypeScript Libraries with No Rust Equivalent

- Find Rust crate equivalents (crates.io)
- Implement functionality using Omnia SDK providers
- Document in Migration.md as manual work

## Related Documentation

- See project rule (`.cursor/rules/project.mdc`) for overall project context
- [Extract SKILL.md](../../spec/skills/extract/SKILL.md) - Source code analysis (moved from this plugin)
- [Wiretapper SKILL.md](../skills/wiretapper/SKILL.md) - Fixture capture from legacy services (also hosts the inlined clone snippet for legacy-repo bootstrap)
- [Replay Writer SKILL.md](../skills/replay-writer/SKILL.md) - Add tests from captured fixtures
- [Analyze SKILL.md](../../spec/skills/analyze/SKILL.md) - Plan-time discovery; hosts the inlined `git clone` snippet that replaced the retired clone skill
- [Specify Artifact Specification](../../references/specify.md) - Artifact format details
