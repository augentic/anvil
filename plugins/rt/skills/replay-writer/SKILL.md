---
name: rt-replay-writer
description: Add tests from real-life JSON fixtures in `tests/data/replay/` to a crate already generated by the Specify workflow, then run tests and review code until they pass. Use when `wiretapper` has captured legacy fixtures and `crate-writer` has produced the target crate, and those fixtures must be folded into regression tests; not for capturing fresh fixtures (that is `wiretapper`).
argument-hint: <crate-name> [project-dir]
---

# Replay Writer Skill

Add integration tests from real-life replay fixtures after `wiretapper` has captured data and `crate-writer` has produced the crate.

## Arguments

```text
$CRATE_NAME   = $ARGUMENTS[0]           # Name of the generated Rust crate (e.g. fleet_api)
$PROJECT_DIR  = $ARGUMENTS[1] OR "."    # Project root (default current directory)
```

Resolve the crate directory using the standard convention `crates/<name>`:

```text
$CRATE_DIR = $PROJECT_DIR/crates/$CRATE_NAME
```

## Prerequisites

- Crate was **generated by the Specify workflow** (e.g. via crate-writer): has `src/`, handlers, and existing `tests/` layout.
- **Real-life test data** has been placed by the user in the crate's replay data folder: `$CRATE_DIR/tests/data/replay/`.
- Fixtures in `tests/data/replay/` follow the [Fixture format](references/fixture-format.md); use the generated [Crate layout](references/crate-layout.md).

## Execution Steps

1. **Inspect crate, instructions, and fixtures**
   - Read `$CRATE_DIR/src/` to identify handlers, input/output types, and provider bounds.
   - Read `$CRATE_DIR/tests/` to see existing test modules and how they use `tests/data/replay/`.
   - If `$CRATE_DIR/tests/data/replay/INSTRUCTIONS.md` exists, read it first. Use its guidance to determine how sample data is loaded, how the MockProvider should be constructed, and any special assertion logic.
   - List `$CRATE_DIR/tests/data/replay/samples/` for shared data files. These are **not** fixtures.
   - List `$CRATE_DIR/tests/data/replay/*.json` and classify each as TestDef-style or raw payload.

2. **Add or extend tests**
   - For each fixture in `tests/data/replay/` that is not yet covered by a test (or that represents a new scenario), add or extend integration tests that:
     - Load the fixture from `tests/data/replay/` using the crate's existing loading style.
     - Apply any `setup` block according to [Fixture format](references/fixture-format.md).
     - If no `setup` block is present, fall back to instructions in `INSTRUCTIONS.md`, then to the crate's existing test patterns.
     - Use the crate's existing `provider.rs` MockProvider and request/response types.
     - Invoke the appropriate handler and assert on response or side effects.
   - Preserve existing test style and naming. Do not remove or weaken existing tests.

3. **Run tests**
   - Run `cargo test` in `$CRATE_DIR`; if any test fails, proceed to step 4.

4. **Review and fix so tests pass**
   - Analyze failures (wrong assertions, missing MockProvider setup, type mismatches, fixture shape vs handler expectations).
   - Apply minimal code or test changes so all tests pass and production code stays guardrail-compliant.
   - Re-run `cargo test` until all tests pass.

5. **Summarize** tests added or updated, failures fixed, and final `cargo test` result.

## References

- [Fixture format](references/fixture-format.md): replay fixture schema, `INSTRUCTIONS.md`, setup blocks, and sample data.
- [Crate layout](references/crate-layout.md): generated crate paths and fixture-to-test mapping.
- Examples: [handler.md](examples/handler.md), [fixtures.md](examples/fixtures.md), [tests.md](examples/tests.md).
- Omnia guardrails: [guardrails.md](../../../omnia/references/guardrails.md).
- Related skills: `crate-writer` produces the crate and initial tests; `code-reviewer` is an optional follow-up for full review.

