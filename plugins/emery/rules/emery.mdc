---
description: Emery plugin context -- Emery workflow orchestration for planning, executing, refining, building, merging, and dropping slices
alwaysApply: true
---

# Emery Plugin - Emery Workflow

## Overview

This plugin provides the core Emery workflow skills: initialize a project, plan a change, approve and execute the plan, refine slices, build them, merge completed slices, drop discarded slices, and finalize. Each skill is an ultrathin wrapper that invokes the matching `emery` CLI verb and relays its output; `/emery:execute` wraps the guest-routed `emery plan execute` loop behind an explicit Gate 1 confirmation.

**Purpose**: Drive the `plan -> execute` rhythm (which itself drives `refine -> build -> merge` per slice), with `drop` for cleanup and `finalize` for closing.

## Source / target adapter structure

Adapters split into two roles:

- **Source adapters** (`adapters/sources/<name>/`) supply input — `survey` (plan time) and `extract` (slice time). Examples: `intent`, `documentation`, `typescript`, `screenshots`.
- **Target adapters** (`adapters/targets/<name>/`) consume artifacts — `guidance`, `build`, and `merge`. Examples: `omnia`, `vectis`, `contracts`.

Each directory contains operation prompts under `prose/prompts/`; adapter identity and metadata come from the compiled component (`describe`), not a manifest file.

## Slice lifecycle

Slices track lifecycle in `metadata.yaml`. The fixed loop is `refine -> build -> merge`, with `drop` as the rollback path:

```text
refining -> refined -> built -> merged
    \          \         \
     `--------> `-------> `-------> dropped
```

- **refining**: Slice directory created; `/emery:refine` extract + synthesis in flight
- **refined**: Proposal, spec, design, and tasks are validated
- **built**: All tasks done, ready to merge
- **merged**: Delta specs merged into baseline, slice archived
- **dropped**: Slice archived without merging into baseline

Per-entry `pending` / `in-progress` / `done` lives in `plan.yaml`, not `metadata.yaml`.

## Workflow

```text
/emery:init <target>               (once per project; for a registry-only workspace, use emery init --workspace)

/emery:plan <name> source ...      (authors change.md + plan.yaml; exits at lifecycle: pending)

/emery:execute                     (Gate 1 on explicit operator confirmation, then the guest-routed
                                   refine -> build -> merge loop; CLI equivalent:
                                   emery plan execute — its first run stamps `approved`)

/emery:finalize <name>             (confirm publication, archive plan)
```

Breakouts invoke the same guest orchestrations one slice at a time, when the execute loop parks or the operator wants to drive a slice by hand:

- **/emery:refine**: For one slice — extract per source, synthesize `proposal.md` / `specs/<domain>/spec.md` / `design.md` / `tasks.md`, validate, transition to `refined` (`emery slice refine`).
- **/emery:build**: Build one slice through its target adapter (`emery slice build`).
- **/emery:merge**: Fold a slice's deltas into the baseline and archive it (`emery slice merge run`).
- **/emery:drop**: Abandon a slice without merging (`emery slice drop`).

## Troubleshooting

### Project not initialized

- Run `/emery:init <target>` in the project root to create `.emery/` and write `project.yaml`
- Verify `.emery/project.yaml` exists

### Slice not found

- Inspect `.emery/slices/` to see active slices
- Check the slice name matches (kebab-case)

### Artifacts incomplete

- Run `emery slice validate <name>` to check artifact status
- Re-run `/emery:refine <name>` to regenerate artifacts for one slice
- Re-run `/emery:plan <name> ...` to author a fresh change

## Related Documentation

- See the project root `AGENTS.md` for overall project context
- [Init SKILL.md](skills/init/SKILL.md) — Initialize Emery
- [Plan SKILL.md](skills/plan/SKILL.md) — Author `change.md` + `plan.yaml`
- [Execute SKILL.md](skills/execute/SKILL.md) — Confirm Gate 1, then drive the drained per-slice loop
- [Refine SKILL.md](skills/refine/SKILL.md) — Synthesize artifacts for one slice
- [Build SKILL.md](skills/build/SKILL.md) — Validate artifacts and implement tasks
- [Merge SKILL.md](skills/merge/SKILL.md) — Finalize completed slices
- [Drop SKILL.md](skills/drop/SKILL.md) — Discard a slice without merging specs
- [Finalize SKILL.md](skills/finalize/SKILL.md) — Close out a change
- [Emery Artifact Guidance Supplement](../../../docs/explanation/augentic-emery-usage.md) — Augentic-specific guidance for proposal / spec / design / tasks

## Specification

Do NOT use `openspec/`, `temp/`, or any other directory convention from other plugins or skills.
