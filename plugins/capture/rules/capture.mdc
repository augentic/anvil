---
description: Runtime capture plugin context -- TypeScript to Rust WASM migration patterns and troubleshooting
alwaysApply: true
---

# Runtime Capture Pointers

- The capture plugin only runs `/capture:wiretapper`; the skill body owns operational steps.
- Static TypeScript extraction lives in the `adapters/sources/code-typescript` source adapter.
- Runtime capture consumption lives in the `adapters/sources/captures` source adapter.
- Replay test generation and verification live in the Omnia target adapter `build` briefs.
- Use this rule as routing context only; load the relevant skill or adapter brief for policy.
